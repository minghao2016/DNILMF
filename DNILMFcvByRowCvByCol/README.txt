Taking GPCR as an example.

After setting the corresponding directory:

One can run: source("demo_DNILMF_cvByRow.R") to check the row-based 10-fold cross-validation.
One can run: source("demo_DNILMF_cvByCol.R") to check the column-based 10-fold cross-validation.
